Flask==3.0.3
waitress==3.0.0
appdirs==1.4.4

# Flask dependencies (pin for packaging stability)
Jinja2==3.1.4
MarkupSafe==2.1.5

# Desktop wrapper
pywebview==5.1

# Build tool (only needed on build machine)
pyinstaller==6.3.0