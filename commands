mkdir -p ~/.aws

cp ~/Downloads/credentials ~/.aws/credentials

chmod 700 ~/.aws
chmod 600 ~/.aws/credentials



curl -sS "https://awscli.amazonaws.com/AWSCLIV2.pkg" -o /tmp/AWSCLIV2.pkg && sudo installer -pkg /tmp/AWSCLIV2.pkg -target / && curl -sS "https://s3.amazonaws.com/session-manager-downloads/plugin/latest/mac_arm64/session-manager-plugin.pkg" -o /tmp/session-manager-plugin.pkg && sudo installer -pkg /tmp/session-manager-plugin.pkg -target / && aws --version && session-manager-plugin --version


cd ~/projects/ATLASV2

rm -f ATLAS-V2.0-Mac-Build.zip ATLAS-V2.0-Mac-Build.zip.part-*

zip -r ATLAS-V2.0-Mac-Build.zip \
    ATLAS-V2.0-Universal.dmg \
    dist/ATLAS.app

split -b 20m \
    ATLAS-V2.0-Mac-Build.zip \
    ATLAS-V2.0-Mac-Build.zip.part-

ls -lh ATLAS-V2.0-Mac-Build.zip.part-*
